akka.actor.deployment {
  /scannerActor/scannerRouter {
      router = consistent-hashing-group
      routees.paths = ["/user/scannerActor"]
      cluster {
        enabled = on
        allow-local-routees = on
      }
    }
}